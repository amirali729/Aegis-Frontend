// Every feature should import `toast` from here rather than from
// `sonner` directly, so the notification library is only referenced
// in one place (see 03-Shared-Architecture.md).
export { toast } from "sonner";